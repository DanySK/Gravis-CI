#!/bin/bash
# Windows Defender is now disabled by default.
echo "Windows defender disabling script is deprecated and currently empty."
echo "Travis now automatically disables Windows Defender"
echo "Please remove this configuration from your build, it will get dropped in future"
