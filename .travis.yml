language: bash
os:
  - linux
  - osx
  - windows
env:
  global:
    - GRAVIS_REPO="git@github.com:DanySK/Gravis-CI.git"
    - GRAVIS="$HOME/gravis"
  matrix:
    - JDK="adopt"
    - JDK="adopt@1.8"
    - JDK="adopt-openj9"
#    - JDK="amazon-corretto"
before_install:
  - export PATH="$GRAVIS:$PATH"
script:
  - install-jdk-travis.sh
  - install-maven.sh
notifications:
  email:
    if: branch = master
    on_success: never
    on_failure: change
